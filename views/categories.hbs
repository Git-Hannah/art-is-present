<h2>Categories</h2>
<div class="categories-container">
    <div class="row">
        <div class="column">
            <img src="{{image_url}}" class="img" alt="{{name}}">
        </div>
        <div class="colum-2">
            <h1>{{name}}</h1>
            <p>{{tagline}}</p>
            <p>{{description}}</p>
        </div>
    </div>
</div>